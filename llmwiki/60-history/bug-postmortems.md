---
canonical-for:
  - "Postmortem catalogue: symptom, true root cause, fix, generalizable lesson"
sources:
  - memory/issue-*.md (one file per issue, named per row)
  - docs/audits/2026-08-02-register-race-performance-audit.md
  - custom_components/eg4_web_monitor/ (fix-symbol presence only)
  - pylxpweb src/pylxpweb/transports/data.py (fix-symbol presence only)
verified-against:
  eg4_web_monitor: 9f6d6e2
  pylxpweb: 204b95d
last-verified: 2026-08-08
see-also:
  - open-contradictions.md
  - superseded-claims.md
---

# Bug postmortems

The failure history of this integration, as recorded by the engineers who fixed each
one. Read it before proposing a fix in a nearby area: several entries recurred because
the earlier refutation was never written down.

## How to read this table

- **The Grade column grades the root-cause claim**, not the fix and not the symptom.
- **This page never grades a register.** Where a root cause turns on what a register is
  or does, the grade is owned by [`40-hardware/registers.md`](../40-hardware/registers.md)
  and the row links to it instead of restating it. A postmortem that carries its own
  register grade drifts away from the register table the moment either side is revised,
  and then one fact answers differently depending on which page you opened.
- Unless a row names another source, the durable source for row `#n` is the GitHub issue
  `#n` and the maintainer's note `memory/issue-<n>-*.md`. That is why the default grade is
  `asserted-unverified`: the account is contemporaneous engineer testimony recorded at fix
  time, not something re-derived here.
- **✔ in the Fix column** means the named symbol was confirmed present (or, where
  stated, confirmed absent) — `verified-against-code`. It verifies only that the fix is
  still there; it does not re-verify the root cause. **Each ✔ is checked in the repo that
  owns the symbol, at that repo's pin:** integration symbols at `eg4_web_monitor@9f6d6e2`,
  and the one library symbol here — `_merge_status_code` in #261 — at
  `pylxpweb@204b95d`, where it is defined in `transports/data.py` and used to build both
  the fault and warning codes. A ✔ against a repo this page does not pin would be
  unverifiable, which is why both are pinned above.
- `#nnn` are GitHub issues in `joyfulhouse/eg4_web_monitor` unless noted.

## Catalogue

| Ref | Symptom | True root cause | Fix | Generalizable lesson | Grade |
|---|---|---|---|---|---|
| **#83** | Setup times out; `battery_soc = 144` | Multiple devices on one Modbus gateway → 60+ sequential reads on first setup → HA ~90 s setup timeout → `CancelledError` leaves the connection dirty | Read budgeting; static first-refresh phase ✔ (`_build_static_local_data`, `_local_static_phase_done`) | Budget Modbus transactions against HA's setup timeout; a cancelled setup poisons the connection | `asserted-unverified` |
| **#158** | Data spikes (battery count 5421, 2996 A) even with validation on; validation on kills GridBOSS | WiFi dongle misroutes cloud responses to the local client — valid CRC, wrong data; missing canaries for count/current; monotonicity wrongly gated by the validation toggle | Cross-request validation (serial / function / start-register vs the request) + new canaries | Validate that the **response matches the request**, not just its checksum | `asserted-unverified` |
| **#159** | LXP single/split-phase users get wrong or no voltage sensors | `FAMILY_DEFAULT_FEATURES[LXP]` hardcoded three-phase; device type codes 12 vs 44 not distinguished; registers 127-128 / 193-194 model-filtered to EG4; mismatch detection overrode the user's config | Per-device-type features, filter removal, phase-neutral aliases, user config authoritative | A family default is not a device capability; never override a user's explicit config with detection | `asserted-unverified` |
| **#170 / #258** | >4 batteries unreliable; the 5th shows data on restart then never again | The captured inverter/dongle Modbus path exposes at most four battery slots, plus rotation; the cloud bank was fetched once at startup and never refreshed; login broke ~96% on `UserVisitRecord` when the last-viewed device is a parallel group; never-evict + supplemental gate + freshness overlay encoded contradictory assumptions and disabled each other | Serial-keyed accumulation ignoring register 96; supplemental cloud battery fetch on its own clock; relative 2-minute freshness gate; Optional login fields | Multiple independent staleness heuristics will fight each other; make freshness relative; a slot count is not a pack count | `asserted-unverified`. The four-slot ceiling is a register fact owned by [`40-hardware/registers.md`](../40-hardware/registers.md) — take its grade **and its scope** from there; the keeper establishes the captured inverter/dongle path only and leaves family portability unresolved |
| **#172** | BMS current limits 10× off | A genuine same-units scale mismatch (`DIV_100` where `DIV_10` was correct) | Correct the scale | The counter-example to `maxChgCurr`: *some* scale disagreements are real | `asserted-unverified` |
| **#218 / #227** | Daily consumption holds at the peak and never resets at midnight (LOCAL only) | An unbounded monotonic clamp on the LOCAL computed consumption ate the legitimate drop to zero | Delete the coordinator clamp ✔ (`_clamp_computed_energy` confirmed **absent**); add the bounded entity-layer guard ✔ (`base_entity.py` → `_guard_total_increasing`) | Integration-level reset guards fight HA's native `total_increasing` reset handling | `asserted-unverified` |
| **#232** | BMS permission flags not surfaced | — (feature) | Register 95 bitmap decode | Produced the standing rule: "confirm readings" means a live cross-check against the cloud field, not unit tests | `asserted-unverified` |
| **#243 (a)** | EPS/grid aggregate voltage unknown in LOCAL while L1/L2 are known | The alias ran only on the deprecated LOCAL path, and `features` was populated only on parameter-refresh polls | Run feature extraction every poll (cheap cached read), then alias | A helper that exists is not a helper that runs — check the *active* call site | `asserted-unverified` |
| **#243 (b)** | PV input currents unavailable | Registers 72-74 exist as candidates but read 0 or garbage on tested EG4 hardware while the array is producing; the cloud carries no field either — the portal derives it | `derive_pv_current(P, V)` in pylxpweb | "Missing" data may be a derivation everywhere, including at the vendor | `asserted-unverified` (issue #243; `memory/issue-243-eps-aggregate-and-pv-current.md`). Register status for I72/I73/I74 is owned by [`40-hardware/registers.md`](../40-hardware/registers.md) — take its grade from there |
| **#243 (c)** | Per-inverter grid L1/L2 read 0 V | Per-leg grid voltage is measured by the GridBOSS, not the inverter; the corresponding inverter registers read 0 on every model tested | Suppress `grid_voltage_l1/l2` when 0/None; leave generator legs alone | Distinguish "0 with no input" from "0 with live input" before suppressing | `asserted-unverified` (issue #243; `memory/issue-243-eps-aggregate-and-pv-current.md`). Register status for I193/I194 is owned by [`40-hardware/registers.md`](../40-hardware/registers.md) — take its grade from there |
| **#251** | Quick Charge Duration does not reflect the device | HOLD 234 is firmware-governed and idle writes are rejected; the entity had been showing a retained preference | Mirror HOLD 234 live; preference is CLOUD-only; paired-frame local start | Mirror the register the firmware owns; do not invent a shadow value | `asserted-unverified` (issue #251; `memory/quick-charge-local-control-registers.md`). H234's grade is owned by [`40-hardware/registers.md`](../40-hardware/registers.md) — take it from there |
| **#252** | Battery devices duplicated after a mode change | Battery identity differed per mode | One serial-first identity with in-place registry migration | Identity must be transport-independent | `asserted-unverified` |
| **#253** | Two "Has Runtime Data" sensors, neither deletable | Two sensor keys shared one display **name** → same entity_id slug, different unique_ids → HA keeps both | Delete the duplicate, auto-purge orphans ✔ (`__init__.py` → `_async_cleanup_duplicate_runtime_data_entities`), add a name-collision regression test | Duplicate *names* create duplicate entities; unique_id uniqueness is not enough | `asserted-unverified` |
| **#256** | One inverter's entities all unavailable while a sibling works | The offline device returns a partial cloud payload; required model fields raised `ValidationError`, which was swallowed → `has_data=False` | Make cloud-omittable live fields Optional | Every cloud live-measurement field must be Optional | `asserted-unverified` |
| **#258** (beta.18) | Subsets of battery entities flip unavailable and return | The HYBRID merge rebuilt the battery dict from the CLOUD payload as baseline each cycle; a momentarily omitted or re-keyed battery vanished | `_apply_battery_carry_forward()` with migration and supersede guards ✔ (`coordinator_http.py` → `_apply_battery_carry_forward`) | Availability by key-presence + a dict rebuilt each cycle = flicker by construction | `asserted-unverified` |
| **#259** | 12000XP (`SNA-US 15K`) shows zero controls in cloud | Control gates matched model-name substrings; some ratings matched by luck (`"12k"`, `"xp"`) | Family backstop ✔ (`utils.py` → `CONTROL_CAPABLE_FAMILIES`, `is_supported_control_model`) | Gate capability on detected family, never on marketing strings | `asserted-unverified` |
| **#261** | Some HYBRID sensors flicker unknown/unavailable | Keys written only when non-None, plus two different availability semantics per base class; the bank gate keyed on unreliable register 96; a bms-only read drop produced a half-empty bank that overwrote cache; `inv if inv else bms` turned a healthy 0 into None | Bank cloud fallback, fault/warning carry-forward, `battery=None` on bms failure, `_merge_status_code` ✔ (pylxpweb `transports/data.py` → `_merge_status_code`) | `0 if 0 else None` — falsy-vs-absent confusion in a merge is a whole bug class | `asserted-unverified` |
| **#262** | — (feature) Operating State | Vendor Table 9 self-contradicts on `0x60` | Off-grid decided by threshold `code >= 0x40`, not a set | Vendor tables contradict themselves; corroborate with a breaker test and a second implementation | `asserted-unverified` |
| **#274** | Grid Sell Back Power shown as a percent | Raw value is in 100 W units | Re-scale to kW; add Fast Zero Export switch | — | `asserted-unverified` |
| **#277 / #295 / #312** | Schedule times wrong | Packed hour (low byte) + minute (high byte) | Time entities per schedule family | — | `asserted-unverified` |
| **#282** | Parameter-backed controls blank for an hour after one failed read | Partial reads replaced last-known values; throttle stamps advanced on incomplete cycles | Merge partials over last-known; stamp only on complete cycles; per-device retry with a 2-minute floor | A throttle stamp must record **success**, not attempt | `asserted-unverified` |
| **#296 / #308** | Quick Charge fails on XP | Register 233 is reported to return ILLEGAL DATA ADDRESS on the off-grid units tested; no raw request/exception-response capture was preserved | Route status and control via cloud | A register that reads on one family may not exist on another | `asserted-unverified` (issues #296, #308). The H233 off-grid access boundary is owned by [`40-hardware/registers.md`](../40-hardware/registers.md) — take its grade from there |
| **#300** | Removed battery pack frozen until restart (LOCAL) | The 6-hour eviction was unreachable on non-empty polls | Unconditional per-merge eviction | Guard conditions on "empty poll" make cleanup unreachable | `asserted-unverified` |
| **#320** | Fast block-size mode permanently latched off | Misrouted/unsolicited dongle frames (heartbeats `0xC1`, parameter frames `0xC3`/`0xC4`) fell through to a generic "frame too short" | Request-aware TCP-function validation → mismatch causes a one-cycle fallback and a ~5-minute re-probe; short reads still latch (that is the real firmware-cap signature) | Classify failures before latching a permanent degradation | `asserted-unverified` |
| **#322** | Refresh Data button does nothing useful | A bare `refresh()` is a cache-respecting no-op and never reads parameters; an obsolete link-down gate blocked the cloud parameter route; `gather(return_exceptions=True)` meant nothing propagated | Force refresh including parameters; raise on incomplete | "Log and raise" is fiction inside a `return_exceptions=True` gather | `asserted-unverified` |
| **#323 / #324** | DST switch pinned off since inception | Three defects: the coordinator never populated `daylightSavingTime`; the hourly sync misread `detect_dst_status()` (it returns the actual state, not match/mismatch); the comparison used a cached flag nothing re-read | Populate, re-read each sync, correct the semantics | Three independent defects can hide behind one symptom; re-read remote state instead of trusting a cache | `asserted-unverified` |
| **#328** | Grid Peak Shaving power blank; writes time out | HR206 is 0.1 kW; firmware NAKs peak-shaving writes and zeroes the setpoint while the mode is off | Local mapping and writes; verify-then-block pre-check with a live register-179 read | A parameter-specific write failure can mean "the owning mode is disabled" | `asserted-unverified` (issue #328; `memory/live-write-window-findings.md`). H206's scaling grade is owned by [`40-hardware/registers.md`](../40-hardware/registers.md) — take it from there |
| **#342** | — (DRY consolidation) | `history_import` timezone change | Re-keyed as a verified migration with a snapshot, marker saved before snapshot delete | "Clear and rebuild" destroys data on partial-range or partial-failure paths; verify before marker | `asserted-unverified` |
| **#345 / #346** | Valid 0.1 kWh daily ticks rejected | IEEE-754: `4.4 - 4.3 == 0.10000000000000053` overshoots a 0.1 floor while `5.1 - 5.0` does not, so rejection depended on the base value — and the existing boundary test passed by luck | `DELTA_FLOAT_TOLERANCE = 1e-6` | `delta > floor` on quantized floats needs a tolerance; choose test pairs that actually overshoot | `asserted-unverified` |
| **#348** | One LOCAL inverter reports unknown for **all** sensors | A no-BMS secondary returned the register-67 "no reading" sentinel, which tripped the runtime temperature canary, so the whole payload was rejected. The register and its sentinel value are owned by [`40-hardware/registers.md`](../40-hardware/registers.md) | Normalize the exact sentinel to None on every read path including the cloud property; keep the canary intact | Excuse the exact sentinel, never widen the canary; a raw cloud model bypasses `__post_init__` | `asserted-unverified` (issue #348; `memory/issue-348-one-inverter-all-unknown.md`) |
| **#353** | Firmware update crashes; entity idles mid-update | `updateStatus='WAITING'` missing from the StrEnum → pydantic `ValidationError`, swallowed by the coordinator; multi-component updates need one `standardUpdate/run` per component; the start bool was discarded | `_missing_` on both enums; orchestrator with step budget, timeouts, start grace and FAILED abort; bounded busy tolerance | Any vendor enum needs an unknown-value fallback; accepted async runs register late, so an early idle poll is not completion | `asserted-unverified` |
| **#362** | Optimistic write state lost or wrongly reverted | Write-OK plus refresh-fail cleared the state; `last_update_success` lies during the 3-strike window | Retained optimistic state with a 300 s TTL ✔ (`RETAINED_OPTIMISTIC_TTL`); detect a stale refresh by data **object identity** | Do not trust an aggregate success flag for a per-write decision | `asserted-unverified` |
| **#367** | Bank current canary false-positives | A fixed threshold against large banks | Scale by battery count (150 A/battery, 500 A floor, 2000 A ceiling) with present-battery corroboration | Canary thresholds must scale with the installation | `asserted-unverified` |
| **#471 / #472** | AC Couple control | The register-179 bit-11 mapping was never toggle-pinned — no named action, no raw before/after pair, no restoration. It ships on lineage inference | Dedicated store plus a write-seed registry | **The risk is live in production, and it is not gated.** In LOCAL and HYBRID this switch writes **local-first with cloud fallback** today — `verified-against-code` (`switch.py` → the AC Couple switch; `utils.py` → `async_write_with_cloud_fallback`). The mitigation the code names — forcing a register-179 re-read after the write — proves storage and transport only; the source says so itself: "a write that lands on the WRONG bit would still ACK, which no readback can catch". That is the #476 mechanism, undischarged, on a bit nothing has pinned. #472 asks for the lockstep toggle that would rule it out; #558 tracks the identical mechanism on register 161; [C7](open-contradictions.md) holds the contradiction open | `asserted-unverified` for the mapping (issues #471, #472; `memory/issue-471-ac-couple-switch.md`). H179 b11's grade is owned by [`40-hardware/registers.md`](../40-hardware/registers.md) — take it from there |
| **#476** | Off-Grid Mode switch does nothing in HYBRID/LOCAL (also the never-root-caused tail of #194) | The register 110 green bit is **14**, not the 8 pylxpweb had mapped and falsely annotated `# verified`; a wrong-bit write is firmware-ACKed, so there is no fallback and no log above DEBUG | Unified register-110 parameter keys plus a per-family contract test ✔ (`switch.py` decodes bit 14) | A writable-but-wrong bit succeeds silently; readback cannot detect it; gating is the only mitigation | `asserted-unverified` (issue #476; `memory/issue-476-green-mode-bit14.md`). The H110 b14 grade and its raw toggle record are owned by [`40-hardware/registers.md`](../40-hardware/registers.md) — take them from there |
| **#478** | HA SOC 90% vs portal 70% | None in the integration — the portal panel reads the very field HA relays | Ask the reporter for the entity_id | Prove source identity before hunting a data bug | `portal-correlated` (portal JS binding read; tri-source live check agreed) |
| **#479** | Sensors froze at pre-outage values when the dongle went offline | The portal serves the last register mirror with `success:true` **and** `lost:true`; nothing consulted `lost`. Separately, a post-reconnect lifetime catch-up delta exceeded the spike cap | Blank measurements to None at four levels with a keep-set; outage-gated cap widening | Freshness must be gated on the source's own liveness verdict; never blank by dropping keys | `portal-correlated` |
| **#485** | Switch writes attempt a local read-modify-write on a known-down link | `_execute_local_with_fallback` lacked the link-down short-circuit its sibling helper had | Parity between the two helpers | When two helpers do "the same thing", diff them | `asserted-unverified` |
| **#490** | Internal temperature reads 0 | A cloud-sourced fake zero — **and the defect splits within device type code 54**, so the proposed `EG4_OFFGRID` gate would have deleted a working 6000XP sensor | No gate, no purge; blank an exact cloud 0 to None, scoped by `transport_runtime is not None` | Two observations do not make a family rule; a wrong purge is irreversible for the user | `asserted-unverified` |
| **#495 / #497 / #499 / #506** | Per-string PV yield on cloud; fake OFF states; smart load; degraded battery vanishing | Fake-confident values and missing known-state requirements | `requires_known_state` ✔ (`switch.py` → `requires_known_state`), `is_absent()`, cloud chart side-fetch | Absent is not off; a degraded device must stay listed | `asserted-unverified` |
| **#510** | Gold coverage CI wedged >90 minutes, three times | Not network I/O — the coordinator builds a real client (Platinum websession injection) and tests hitting cloud paths burned pure wall clock in exponential-backoff `asyncio.sleep`s | Autouse fixture refusing `_request` **and** `_get_session` (two direct-session bypasses) plus tripwires; 25:21 → 1:50 | A "network" symptom in tests may be sleeps, not sockets; guard every bypass path | `asserted-unverified` |
| **#511 / #516** | Cloud side-fetch outages hang cycles | A gather with `return_exceptions=True` plus store getters returning truthy **all-None schema dicts** on total failure hid failures inside normal returns, so the breaker never opened | Shared breaker ✔ (`coordinator_mixins.py` → `_breakered_cloud_call`); only a non-None value proves reachability (False and 0 count) | A truthiness check on a schema-shaped result is not a reachability check | `asserted-unverified` |
| **#514** | Bank Capacity Percent always 0 on a 12000XP (cloud) | The device reports no battery array to the cloud, so array-computed fields freeze at 0 while the BMS pair stays live | Raw non-zero primary; derive from `cur/max` only on 0/None | Detect a fake zero by contradiction with a live sibling field | `asserted-unverified` |
| **#544** | Generator Power reads 28,646 W with no generator | Off-grid firmware repurposes input register 123 as an ARM-local ~1 Hz seconds counter (one increment site and one memset, disassembled); 124/125/126 are status bitfields, not energy. Hybrid register 123 **is** genuine generator power | Suppress the three sensors on `EG4_OFFGRID` plus purge and Repairs; **scope the registers in the library's decode layer** | The integration reads transport dataclasses directly, so a property-only library fix ships nothing for LOCAL/HYBRID — fix at the decode layer | `asserted-unverified` (issue #544; `memory/issue-544-generator-power-offgrid.md`). The I123 grades — off-grid counter and hybrid generator power, per family — are owned by [`40-hardware/registers.md`](../40-hardware/registers.md) |
| **pylxpweb cloud writes** | "Cloud raw-register writes fail parameter-specifically" (for years) | `write_parameters` form-encoded a dict, so aiohttp serialized `data=<key>&data=<key>` and dropped every value. Broken since inception, and it looked successful | Map register → named write with correct scaling | Only a delta test (write → readback → restore) proves a write path works | `asserted-unverified` (established by delta test against the live portal) |
| **per-transport interval** | With two or more same-type local devices, only the first is ever polled | `_should_poll_transport()` used one shared timestamp per transport **type** but was called per device | Pre-compute pollable types once per cycle | A shared throttle stamp consumed in a per-item loop starves every later item | `asserted-unverified` |
| **INT-01** (2026-08-02 audit) | Mixed Modbus TCP + serial: the later type starved every cycle | The same class as above, across cadence classes | One due-decision per cadence class plus an explicit never-polled sentinel | — | `asserted-unverified` (source: `docs/audits/2026-08-02-register-race-performance-audit.md`) |

## Cross-cutting patterns

Recurrences, not one-offs. Each has bitten more than once.

| Pattern | Instances | Rule |
|---|---|---|
| **Fake-confident zero** — the cloud returns a structurally valid 0 for a field it cannot compute | #490, #497, #514 | Detect by contradiction with a live sibling value; derive or blank, never trust the 0 |
| **Required field on an optional payload** — a partial cloud response raises `ValidationError`, gets swallowed, and blanks a device | #256 (`InverterRuntime`/`BatteryInfo`), #258 (`UserVisitRecord` on login), #353 (`UpdateStatus` enum) | Every cloud live-measurement field Optional; every vendor enum gets an unknown-value fallback |
| **Swallowed failure** — an exception or an all-None result passes as success | #322, #348, #511/#516 | `return_exceptions=True` means nothing propagates; a truthy schema-shaped dict is not reachability |
| **Falsy vs absent** — `0`, `False`, `None` and "key missing" conflated | #261 (`inv if inv else bms`), #511 | Compare against None explicitly in merges and reachability checks |
| **Availability by key-presence** — a dropped key becomes an unavailable entity | #253, #258 beta.18, #261, #479 | Extract-then-null; never gate data by dropping keys |
| **Contradictory staleness heuristics** — independent freshness rules disable each other | #170/#258 | Before adding a staleness rule, check what the existing rules already assume; bound every never-evict rule |
| **Capability gated on the wrong grain** — model string, or a family where the defect splits inside it | #259 (too coarse a string), #490 (family too coarse), #544 (family gate correct) | Gate on family; verify the split before generalising from two observations |
| **Throttle/stamp bugs** — a stamp recording attempt rather than success, or a `0.0` monotonic default | #282, #378/#380 and five more sites found in the 2026-08-02 audit (INT-08) | Stamp only on success; use a `None` sentinel for "never ran" — `time.monotonic()` is host uptime, so `0.0` reads as throttled on every freshly booted host |
| **Write path never delta-tested** — a success-shaped API sending garbage, or writing a wrong-but-writable bit | pylxpweb cloud writes, #476 | A successful write proves format acceptance, not targeting |
